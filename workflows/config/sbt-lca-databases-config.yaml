# The relative path to the directory of sketch databases 
dirpath: dbs

# The sketch databases to convert filename
database:
  - db.plastid.2.1.sig.gz

# The k values within the database (`sourmash sig fileinfo`)
k-size:
  - 21
  - 31
  - 51

scale:
  - 100
  - 1000
